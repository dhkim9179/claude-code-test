<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.MileageHistoryMapper">

    <!-- Result Map 정의 -->
    <resultMap id="MileageHistoryResultMap" type="com.example.demo.domain.MileageHistory">
        <id property="id" column="id"/>
        <result property="mileageMemberId" column="MILEAGE_member_id"/>
        <result property="type" column="type"/>
        <result property="amount" column="amount"/>
        <result property="description" column="description"/>
        <result property="createDate" column="create_date"/>
    </resultMap>

    <!-- 마일리지 이력 ID로 조회 -->
    <select id="findById" resultMap="MileageHistoryResultMap">
        SELECT id,
               MILEAGE_member_id,
               type,
               amount,
               description,
               create_date
        FROM MILEAGE_HISTORY
        WHERE id = #{id}
    </select>

    <!-- 회원 ID로 마일리지 이력 목록 조회 -->
    <select id="findByMemberId" resultMap="MileageHistoryResultMap">
        SELECT id,
               MILEAGE_member_id,
               type,
               amount,
               description,
               create_date
        FROM MILEAGE_HISTORY
        WHERE MILEAGE_member_id = #{memberId}
        ORDER BY create_date DESC
    </select>

    <!-- 회원 ID와 유형으로 마일리지 이력 목록 조회 -->
    <select id="findByMemberIdAndType" resultMap="MileageHistoryResultMap">
        SELECT id,
               MILEAGE_member_id,
               type,
               amount,
               description,
               create_date
        FROM MILEAGE_HISTORY
        WHERE MILEAGE_member_id = #{memberId}
          AND type = #{type}
        ORDER BY create_date DESC
    </select>

    <!-- 마일리지 이력 전체 조회 (페이징) -->
    <select id="findAllWithPaging" resultMap="MileageHistoryResultMap">
        SELECT id,
               MILEAGE_member_id,
               type,
               amount,
               description,
               create_date
        FROM MILEAGE_HISTORY
        ORDER BY create_date DESC
        OFFSET #{offset} ROWS
        FETCH NEXT #{limit} ROWS ONLY
    </select>

    <!-- 마일리지 이력 등록 -->
    <insert id="insert" parameterType="com.example.demo.domain.MileageHistory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO MILEAGE_HISTORY (
            MILEAGE_member_id,
            type,
            amount,
            description,
            create_date
        ) VALUES (
            #{mileageMemberId},
            #{type},
            #{amount},
            #{description},
            #{createDate}
        )
    </insert>

    <!-- 마일리지 이력 삭제 -->
    <delete id="deleteById">
        DELETE FROM MILEAGE_HISTORY
        WHERE id = #{id}
    </delete>

    <!-- 회원의 전체 마일리지 이력 수 조회 -->
    <select id="countByMemberId" resultType="int">
        SELECT COUNT(*)
        FROM MILEAGE_HISTORY
        WHERE MILEAGE_member_id = #{memberId}
    </select>

    <!-- 마일리지 이력 배치 등록 -->
    <insert id="batchInsert">
        INSERT INTO MILEAGE_HISTORY (MILEAGE_member_id, type, amount, description, create_date)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.mileageMemberId}, #{item.type}, #{item.amount}, #{item.description}, #{item.createDate})
        </foreach>
    </insert>

</mapper>
